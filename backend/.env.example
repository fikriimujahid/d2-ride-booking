# Server
PORT=3000
NODE_ENV=development

# Database
# Example: postgresql://postgres:postgres@localhost:5432/ride_booking
DATABASE_URL=
DB_SSL=false

# Auth (JWT)
# Used by middleware that verifies Authorization: Bearer <token>
JWT_SECRET=dev-only-change-me-please
JWT_ISSUER=
JWT_AUDIENCE=
